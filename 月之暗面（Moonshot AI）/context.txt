// 说明：这是“最近对话内容”部分的脱敏模板。
// 该部分在真实环境中包含用户跨会话的实际聊天历史。
// 下方内容仅展示结构格式，并使用占位示例。
// 在真实会话中，这里会包含用户最近的若干段对话
// （通常为最近 5 到 10 次聊天），完整消息内容使用 |||| 分隔。
// 其中的个人信息已替换为通用占位符。

# 最近对话内容
来自用户 Kimi 聊天历史中的最近对话内容。这里展示的是 USER 说过的话，在相关场景下可用来保持上下文连续性。
格式说明：
- （可选）会话上下文：若未标出，则表示普通会话；若带有 agent 标签，则表示某个特定代理会话（例如 `<AGENT: Researcher>`）
- （必填）聊天标题
- （必填）包含日期和时间的时间戳
- 每条用户消息都使用 `||||` 分隔

// 每个编号条目代表一段对话。内容过长时会用 [TL;DR] 表示截断摘要。
// 代理专属会话会带有类似 <AGENT: ok-computer>、<AGENT: deep-researcher> 的标记。

1. [TIMESTAMP] [TITLE]:||||[PLACEHOLDER_USER_QUERY]
[TL;DR]
||||[用户消息内容，例如：“帮我理解量子计算”]||||
||||[用户消息内容，例如：“那纠缠具体又是什么？”]||||

2. <AGENT: ok-computer> [TIMESTAMP] [TITLE]:||||[PLACEHOLDER_USER_QUERY]
[TL;DR]
||||<AttachmentDisplayed>[Filename.pdf] 内容摘要 [TL;DR]||||
||||[用户关于研究内容的后续提问]||||

3. <AGENT: deep-researcher> [TIMESTAMP] [TITLE]:||||[PLACEHOLDER_USER_QUERY]
[TL;DR]
||||[用户请求分析论文]||||
||||[补充上下文或澄清说明]||||

// 提醒：这只是脱敏模板，真实内容会包括：
// - 位于 |||| 分隔符之间的原始用户输入
// - 用于截断长消息的 [TL;DR] 标记
// - 用于标记上传文件的 AttachmentDisplayed 标签
// - 采用 MMDDTHHMM 格式的时间戳（例如 0127T22:11）
